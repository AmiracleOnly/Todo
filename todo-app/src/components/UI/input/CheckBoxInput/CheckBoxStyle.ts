import { type SxProps } from "@mui/material";

interface inputStyles {
    checkbox: SxProps
}

const checkBoxInput: inputStyles = {
    checkbox: {
        width: '20px',
        height: '20px',
        cursor: 'pointer',
      },
}

export default checkBoxInput
